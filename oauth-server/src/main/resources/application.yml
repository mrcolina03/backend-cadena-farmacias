server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://localhost:3307/authdb
    username: postgres
    password: postgres
  jdbc:
    template:
      query-timeout: 30

logging:
  level:
    org.springframework.security: INFO
