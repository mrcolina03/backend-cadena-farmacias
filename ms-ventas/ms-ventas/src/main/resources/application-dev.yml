server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/ventas_db
    username: root
    password: abcd

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000

inventario:
  url: http://localhost:8082
