server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  security:
# Security config removed to rely on JWK Set URI from environment


inventario:
  url: http://ms-inventario:8082
